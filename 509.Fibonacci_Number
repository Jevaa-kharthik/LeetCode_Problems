class Solution(object):
    store = {}
    def fib(self, n):
        """
        :type n: int
        :rtype: int
        """
        if n == 0: return 0
        if n == 1: return 1
        if n in self.store:return self.store[n]
        result = self.fib(n-1) + self.fib(n-2)
        self.store[n] = result
        return result

#This Code runs in time and space complexity
#This took 5ms Runtime
